package components;

import org.apache.log4j.Logger;
import org.openqa.selenium.WebDriver;

public class Button extends AComponent {


    public Button(WebDriver driver, Logger logger) {
        super(driver, logger);
    }
}
